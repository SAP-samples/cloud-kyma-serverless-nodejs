 const {hdbext,hanaConfig} = require('./config')
hdbext.createConnection(hanaConfig, function(error, client) { 
    if (error) {
      return console.error(error);
    }
  client.exec("CREATE TABLE rooms (id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, name VARCHAR(128), time VARCHAR(128))", function (err, result) {
    if (err) throw err;
    console.log("table created")
});    
client.exec("CREATE TABLE game (ID INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, name VARCHAR(128), score INTEGER, TIME INTEGER)", function (err, result) {
  if (err) throw err;
  console.log("table created")
});
client.exec(`INSERT INTO game (name,score,time) VALUES('admin',0,0)`, function (err, rows) {
    if (err) throw err;
        console.log(' rows:', rows);
  });
// client.exec("SELECT * FROM game", function (err, rows) {
//     if (err) throw err;
//     console.log(rows);
// });
 });